apiVersion: v1
kind: Service
metadata:
  name: git-ops-init-canary
  labels:
    app: git-ops-init-canary
spec:
  selector:
    app: git-ops-init-canary
  ports:
    - port: 80
      targetPort: 8080
