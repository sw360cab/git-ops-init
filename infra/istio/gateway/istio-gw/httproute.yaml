apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-istio
spec:
  parentRefs:
  - name: git-ops-istio
  rules:
  - backendRefs:
    - name: git-ops-init
      port: 80
      # weight: 90